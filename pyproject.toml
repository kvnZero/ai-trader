[project]
name = "trader"
version = "0.1.0"
description = "Flask app with Tailwind CSS managed by uv"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "akshare>=1.18.60",
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "ruff>=0.11.10",
]

[tool.uv]
package = false
